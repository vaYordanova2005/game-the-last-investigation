# Local project notes/history — never pushed
CLAUDE.md

# Unreal Engine
Binaries/
Intermediate/
Saved/
DerivedDataCache/
Build/

# Per-user editor scratch, not shared
Content/Developers/
Content/Collections/

# Raw downloaded texture source files. The imported .uasset in Content/ is what the game uses;
# these are just the originals, and Tools/fetch_textures.sh re-downloads them on demand.
Art/Source/

# Visual Studio
.vs/
*.sln
*.suo
*.VC.db
*.opensdf
*.sdf
*.opendb
*.ipch

# Python bytecode
__pycache__/
*.pyc
